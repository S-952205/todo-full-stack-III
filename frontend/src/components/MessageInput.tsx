// We no longer need this custom component since we're using ChatKit
// This component is now obsolete as ChatKit provides its own input
// Keeping this file temporarily in case we need to reference the logic
export {};